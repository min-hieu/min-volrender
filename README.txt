#min-VolRenderer

minimal volume renderer written by Charlie.

This codebase focuses on:
- WebGL2 APIs
- minimal abstraction
- most performance for the least code
- minimal dependencies (only `gl-matrix` for linalg)
- mostly functional style (except camera and input handling)
